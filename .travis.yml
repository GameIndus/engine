language: node_js
dist: xenial

node_js:
  - "12"

addons:
  sonarcloud:
    organization: "gameindus"

install:
- npm install

script:
- npm run build
- npm run testWithCoverage
- sonar-scanner

cache:
  directories:
  - node_modules
  - node_cache
  - $HOME/.npm
  - $HOME/.sonar/cache
